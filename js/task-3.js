function getElementWidth(content, padding, border) {
  const content1 = parseInt(content);
  const padding1 = parseInt(padding);
  const border1 = parseInt(border);
  console.log(`${content1 + padding1 * 2 + border1 * 2}`);
}
